/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./src/**/*.{js,jsx,ts,tsx}"], // Tailwind auf React-Komponenten anwenden
    theme: {
        extend: {}, // Für spätere Anpassungen
    },
    plugins: [],
};
